name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api:
    name: API · lint + build + test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        working-directory: apps/api
        run: bun run lint

      - name: Build
        working-directory: apps/api
        run: bun run build

      - name: Unit tests
        working-directory: apps/api
        run: bun run test

      - name: E2E tests
        working-directory: apps/api
        run: bun run test:e2e

  career-api:
    name: Career API · lint + typecheck + build + test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        working-directory: apps/career-api
        run: bun run lint

      - name: Type check
        working-directory: apps/career-api
        run: bun run check-types

      - name: Build
        working-directory: apps/career-api
        run: bun run build

      - name: Unit tests
        working-directory: apps/career-api
        run: bun run test

      - name: E2E tests
        working-directory: apps/career-api
        run: bun run test:e2e

  career-web:
    name: Career Web · lint + typecheck + build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        working-directory: apps/career-web
        run: bun run lint

      - name: Type check
        working-directory: apps/career-web
        run: bun run check-types

      - name: Build
        working-directory: apps/career-web
        run: bun run build

  terraform:
    name: Terraform · fmt + validate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: packages/terraform-aws
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8
          terraform_wrapper: false

      - name: Format check
        run: terraform fmt -check -recursive

      - name: Init (no backend)
        run: terraform init -backend=false -input=false
        env:
          AWS_REGION: us-east-1

      - name: Validate
        run: terraform validate -no-color
        env:
          AWS_REGION: us-east-1
