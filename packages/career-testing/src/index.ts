/**
 * Shared testing utilities for the career-platform workspaces.
 *
 * Fixtures, factories, and test helpers are added here alongside the features
 * that need them (auth contexts, seeded users, fake clock, etc.). Keeping them
 * in one package stops each app from re-inventing test scaffolding.
 */
export const TESTING_PACKAGE_VERSION = '0.0.0' as const;
